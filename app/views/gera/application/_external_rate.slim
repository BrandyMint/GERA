- object = Gera::ExternalRateDecorator.decorate external_rate

h2 Внешний курс (#{object.id})
- columns = %i(id snapshot created_at source rate_value) # sell_price buy_price)

= render 'attributes_table', object: object, columns: columns
